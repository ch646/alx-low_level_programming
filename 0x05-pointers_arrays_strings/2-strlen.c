#include "main.h"
#include <string.h>
/**
 *int _strlen- function that returns the length of a string
 *
 *@s:strlen
 *
 */
int _strlen(char *s)
{
	int count = 0;
	while (*s != '\0')

	count++;
	s++;
	{
	return count;
	}
}
